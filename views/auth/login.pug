extends ../layouts/main

block content
  .login-page
    .login-card
      .login-logo
        h1
          span.text-primary NYO
          span.text-secondary NDOSTOCK
        p.text-muted Hardware Inventory & Sales System
        p.text-muted.small NYONDO General Hardware LTD
      
      if error && error.length
        .alert.alert-danger.mb-3
          i.bi.bi-exclamation-triangle.me-2
          = error[0]
      
      form(method="POST" action="/login")
        .mb-3
          label.form-label Email Address
          .input-group
            span.input-group-text
              i.bi.bi-envelope
            input.form-control(type="email" name="email" placeholder="admin@nyondo.com" required)
        .mb-4
          label.form-label Password
          .input-group
            span.input-group-text
              i.bi.bi-lock
            input.form-control(type="password" name="password" placeholder="Enter password" required)
        button.btn.btn-dark.w-100.fw-bold(type="submit")
          i.bi.bi-box-arrow-in-right.me-2
          | LOGIN
      .mt-3.text-center
        a.btn.btn-outline-dark.btn-sm(href="/")
          i.bi.bi-arrow-left.me-1
          | Back to Home
